{
  "name": "OGSpy Development Environment",
  "dockerComposeFile": "docker-compose.yml",
  "service": "web",
  "workspaceFolder": "/var/www/html",

  "features": {
    "ghcr.io/devcontainers/features/common-utils:2": {
      "installZsh": true,
      "configureZshAsDefaultShell": true,
      "installOhMyZsh": true,
      "upgradePackages": true,
      "username": "root"
    }
  },

  "customizations": {
    "vscode": {
      "extensions": [
        "bmewburn.vscode-intelephense-client",
        "ms-vscode.vscode-json",
        "formulahendry.auto-rename-tag",
        "christian-kohler.path-intellisense",
        "xdebug.php-debug"
      ],
      "settings": {
        "php.validate.executablePath": "/usr/local/bin/php",
        "php.debug.executablePath": "/usr/local/bin/php",
        "intelephense.environment.phpVersion": "8.4.0"
      }
    }
  },

  "forwardPorts": [8080, 8081, 3306],
  "portsAttributes": {
    "8080": {
      "label": "OGSpy Web Server (Apache)"
    },
    "8081": {
      "label": "phpMyAdmin"
    },
    "3306": {
      "label": "MariaDB Database"
    }
  },

  "postCreateCommand": "apt-get update && apt-get install -y libpng-dev libonig-dev libxml2-dev libzip-dev zip unzip mariadb-client && docker-php-ext-install mysqli pdo_mysql mbstring xml zip bcmath gd && pecl install xdebug && docker-php-ext-enable xdebug && a2enmod rewrite && service apache2 restart && echo 'xdebug.mode=debug,coverage' >> /usr/local/etc/php/conf.d/docker-php-ext-xdebug.ini && echo 'xdebug.start_with_request=yes' >> /usr/local/etc/php/conf.d/docker-php-ext-xdebug.ini && echo 'xdebug.client_host=host.docker.internal' >> /usr/local/etc/php/conf.d/docker-php-ext-xdebug.ini && echo 'xdebug.client_port=9003' >> /usr/local/etc/php/conf.d/docker-php-ext-xdebug.ini",

  "remoteUser": "root"
}
