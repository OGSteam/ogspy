name: Docker Image OGSPY

on:
  push:
    branches: [ feature/github_action ]
  pull_request:
    branches: [ feature/github_action ]
env:
  # TODO: Change variable to your image's name.
  IMAGE_NAME: ogspy

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: docker build . --file .docker/Dockerfile --tag $IMAGE_NAME
    - name: Docker Login
      if: success()
        docker login ${{ secrets.DOCKER_REGISTRY_URL }} --username ${{ secrets.DOCKER_USERNAME }} --password "${{ secrets.DOCKER_PASSWORD }}"
       # docker push docker.pkg.github.com/repoowner/repo_name/image_name:tag

    - name: Push image
      if: success()
      run: |
        IMAGE_ID=${{ secrets.DOCKER_REGISTRY_URL }}/${{ secrets.DOCKER_REPOSITORY_NAME }}/$IMAGE_NAME
        # Strip git ref prefix from version
        VERSION=$(echo "${{ github.ref }}" | sed -e 's,.*/\(.*\),\1,')
        # Strip "v" prefix from tag name
        [[ "${{ github.ref }}" == "refs/tags/"* ]] && VERSION=$(echo $VERSION | sed -e 's/^v//')
        # Use Docker `latest` tag convention
        [ "$VERSION" == "master" ] && VERSION=latest
        echo IMAGE_ID=$IMAGE_ID
        echo VERSION=$VERSION

        docker tag image $IMAGE_ID:$VERSION
        docker push $IMAGE_ID:$VERSION
        
#https://www.learncloudnative.com/blog/2020-02-20-github-action-build-push-docker-images/
